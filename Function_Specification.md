# 기능 명세서

이 문서는 Image2Graph 애플리케이션의 주요 기능을 설명합니다.

## 1. 핵심 기능

### 1.1. 차트 생성

- **이미지로부터 생성**
  - 사용자는 로컬 컴퓨터에서 차트 이미지(스크린샷, 사진 등)를 업로드할 수 있습니다.
  - 이미지는 드래그 앤 드롭 또는 파일 선택기를 통해 업로드됩니다.
  - 업로드된 이미지는 백엔드 API(`POST /api/convert`)로 전송되어 AI에 의해 분석되고, Vega-Lite JSON 명세로 변환됩니다.
  - 변환된 차트는 화면의 차트 뷰에 렌더링됩니다.

- **템플릿으로부터 생성**
  - 사용자는 미리 정의된 차트 템플릿 목록(예: 막대 차트, 라인 차트, 파이 차트 등)에서 하나를 선택하여 새 프로젝트를 시작할 수 있습니다.
  - 템플릿 선택 시, 해당 템플릿의 기본 Vega-Lite 명세를 기반으로 차트가 즉시 생성됩니다.

### 1.2. 차트 편집

- **AI를 통한 편집 (자연어)**
  - 사용자는 텍스트 입력 필드에 자연어 명령을 입력하여 현재 차트를 수정할 수 있습니다. (예: "색상을 초록색으로 변경해줘", "막대를 가로로 만들어줘")
  - 이 명령은 현재 Vega-Lite 명세와 함께 백엔드 API(`POST /api/edit`)로 전송되어 AI가 명세를 수정하고, 수정된 결과가 차트 뷰에 반영됩니다.

- **인스펙터(Inspector)를 통한 직접 편집**
  - 화면 우측의 인스펙터 패널을 통해 차트의 주요 속성을 직접 수정할 수 있습니다.
  - 수정 가능한 속성:
    - 차트 제목 (Title)
    - X축 필드 (X Field)
    - Y축 필드 (Y Field)
    - 색상 필드 (Color Field)
    - 배경색 (Background)

### 1.3. 차트 데이터 질의응답

- 사용자는 현재 차트 데이터에 대해 자연어로 질문할 수 있습니다.
- 질문은 백엔드 API(`POST /api/ask`)로 전송되며, AI가 Vega-Lite 명세의 데이터를 분석하여 답변을 생성합니다.
- (예: "가장 높은 값은 무엇이야?", "A 카테고리의 값은 얼마야?")

### 1.4. 내보내기 및 저장

- **JSON으로 내보내기**: 현재 차트의 Vega-Lite 명세(.json)를 다운로드할 수 있습니다.
- **CSV로 내보내기**: 차트의 기반이 되는 데이터(`spec.data.values`)를 CSV 파일(.csv)로 다운로드할 수 있습니다.
- **프로젝트 저장**: 현재 작업 상태(차트 명세, 이름 등)는 브라우저의 로컬 저장소(IndexedDB)에 자동으로 저장되어 나중에 다시 열 수 있습니다.

## 2. 화면 구성 및 사용자 인터페이스

### 2.1. 홈 대시보드 (`HomeDashboard.tsx`)

- **초기 화면**: 앱의 기능 소개, 빠른 시작 메뉴(새 프로젝트, 템플릿, 프로젝트 목록)를 제공합니다.
- **프로젝트 목록**: 저장된 모든 프로젝트를 카드 형태로 보여줍니다. 검색 및 정렬(최신순, 이름순) 기능을 제공합니다.
- **템플릿 목록**: 사용 가능한 모든 차트 템플릿을 보여주고, 검색 기능을 제공합니다.

### 2.2. 생성 페이지 (`CreatePage.tsx`)

- 새 차트 생성을 위한 전용 페이지입니다.
- 이미지 업로드(드래그 앤 드롭 지원) 및 자연어 명령 입력 기능을 제공합니다.

### 2.3. 사이드바 (`Sidebar.tsx`)

- 앱의 주요 섹션 간 이동을 위한 최상위 내비게이션을 제공합니다.
- 메뉴: 홈, 만들기, 프로젝트, 템플릿

### 2.4. 차트 뷰 (`ChartView.tsx`)

- 생성되거나 편집된 Vega-Lite 명세를 시각적인 차트로 렌더링하는 영역입니다.
- 차트 확대/축소 및 이동(Zoom/Pan) 기능을 지원합니다.

### 2.5. 인스펙터 (`Inspector.tsx`)

- 차트의 세부 속성을 직접 수정할 수 있는 UI 컴포넌트가 모여있는 우측 패널입니다.

## 3. 프로젝트 관리

- **프로젝트 생성**: 새 이미지를 업로드하거나 템플릿을 선택하면 새 프로젝트가 생성됩니다.
- **프로젝트 열기**: 프로젝트 목록에서 특정 프로젝트를 클릭하면 해당 작업을 이어서 할 수 있습니다.
- **프로젝트 이름 변경**: 프로젝트 목록에서 프로젝트의 이름을 수정할 수 있습니다.
- **프로젝트 삭제**: 프로젝트 목록에서 더 이상 필요 없는 프로젝트를 삭제할 수 있습니다.
