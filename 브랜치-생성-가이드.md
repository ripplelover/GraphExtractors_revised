# 새 브랜치 생성 및 GitHub 푸시 가이드

## 방법 1: 배치 스크립트 사용 (권장)

프로젝트 루트 디렉토리에 있는 `create-branch-and-push.bat` 파일을 더블클릭하여 실행하세요.

스크립트가 자동으로:
1. Git 저장소 초기화 (필요한 경우)
2. 리모트 저장소 추가 (필요한 경우)
3. 새 브랜치 생성
4. 변경사항 커밋
5. GitHub에 푸시

## 방법 2: 수동 명령어 실행

터미널(CMD 또는 PowerShell)에서 다음 명령어를 순서대로 실행하세요:

### 1. 프로젝트 디렉토리로 이동
```bash
cd D:\GraphExtractors_revised-refactor
```

### 2. Git 저장소 초기화 (처음인 경우)
```bash
git init
```

### 3. 리모트 저장소 추가 (처음인 경우)
```bash
git remote add origin https://github.com/ripplelover/GraphExtractors_revised.git
```

### 4. 새 브랜치 생성 및 전환
```bash
git checkout -b feature/create-page-updates
```
(브랜치 이름은 원하는 대로 변경 가능합니다)

### 5. 변경사항 추가
```bash
git add .
```

### 6. 커밋
```bash
git commit -m "Update CreatePage component and related features"
```

### 7. GitHub에 푸시
```bash
git push -u origin feature/create-page-updates
```

## 브랜치 이름 예시

- `feature/create-page-updates` - CreatePage 관련 기능
- `feature/new-feature` - 새 기능
- `fix/bug-fix` - 버그 수정
- `refactor/code-cleanup` - 코드 리팩토링

## 주의사항

1. GitHub 인증이 필요할 수 있습니다 (Personal Access Token 또는 SSH 키)
2. 리모트 저장소에 이미 같은 이름의 브랜치가 있으면 충돌이 발생할 수 있습니다
3. 첫 푸시 시 `-u` 옵션으로 upstream을 설정하면 이후 `git push`만으로 푸시할 수 있습니다

